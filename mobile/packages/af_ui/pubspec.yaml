name: af_ui
description: Giao diện dùng chung — theme sáng/tối, chữ Hán (HanziText), khung shell, hộp thoại, thông báo lỗi.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"
dependencies:
  flutter:
    sdk: flutter
  af_core: ^0.1.0
  flutter_riverpod: 3.4.3
  # TTS tiếng Trung (hợp đồng §5.3.2 ghim 4.2.5; RK-M6). Android/iOS/web.
  flutter_tts: 4.2.5
dev_dependencies:
  af_lints: ^0.1.0
  flutter_test:
    sdk: flutter
  mocktail: 1.0.5
