name: af_auth
description: Phiên đăng nhập mobile dùng chung — token store an toàn, làm mới single-flight, AuthController, màn đăng nhập/đăng ký.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"
dependencies:
  flutter:
    sdk: flutter
  af_core: ^0.1.0
  af_ui: ^0.1.0
  dio: 5.11.1
  flutter_riverpod: 3.4.3
  flutter_secure_storage: 11.2.0
  flutter_timezone: 5.1.0
  go_router: 18.0.1
dev_dependencies:
  af_lints: ^0.1.0
  fake_async: 1.3.3
  flutter_test:
    sdk: flutter
  mocktail: 1.0.5
