// Liên kết ngoài hiển thị trong app — đặt ở `lib/config/` vì đây là nơi DUY NHẤT được chứa URL
// (luật `hardcoded-url` của `tool/check_conventions.dart`).

/// Bản web của app tiếng Trung — nơi DUY NHẤT có màn quản trị nội dung/người dùng (RM-S6).
const kWebAppUrl = 'https://chinese.antfarms.xyz';
