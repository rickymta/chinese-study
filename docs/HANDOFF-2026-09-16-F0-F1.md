# HANDOFF 16/09/2026 — F0 + F1 xong, chuyển sang MacBook

> Đọc file này TRƯỚC khi làm tiếp trên máy mới. Nguồn sự thật: `CLAUDE.md`, `docs/agents/AGENT-WORKFLOW.md`,
> hợp đồng `docs/agent-workflow/2026-09-16-antfarm-nen-tang-tieng-trung-mvp-hop-dong-thuc-thi.md`.

## 1. Trạng thái commit (nhánh `develop`, CHƯA push — repo chưa có remote)

| Commit | Nội dung |
|---|---|
| `14e7024` | Bộ agent chuyển từ mdt-re-construct |
| `a3d701a` | Hợp đồng MVP nền tảng AntFarm + đổi tên agent sang AntFarm/`@af` |
| `2387026` | **F0** — khung backend (19 project), gateway YARP, identity-service + chinese-backend (health + `/api/system/info`), Docker/nginx/certbot, compose dev Postgres + MinIO |
| `6bed574` | **F1** — khung frontend `@af/tsconfig|ui|api`, `apps/chinese`, `lint:ui`, Dockerfile + nginx.conf |
| (commit handoff) | File này + bật `depends_on: chinese-frontend` cho nginx |

Đưa repo sang MacBook: copy nguyên thư mục (bỏ `node_modules`, `bin`, `obj`) **hoặc** người dùng tự thêm remote riêng rồi push/clone. Agent **không push**.

## 2. Đã kiểm / CHƯA kiểm

**Đã kiểm trên Windows:**
- `dotnet build backend/backend.slnx -v q` 0 error; `dotnet test backend/backend.slnx` 16/16 xanh.
- Chạy thật 3 tiến trình: `localhost:5280/identity/api/system/info` + `/chinese/api/system/info` đúng; tắt chinese-backend ⇒ 502, identity vẫn 200; thiếu `appsettings.Development.json` ⇒ báo lỗi tiếng Việt; `5282/scalar/v1` mở được.
- Frontend: `yarn install`, `tsc -b`, `build`, `lint:ui` sạch; review F1 PASS; mô phỏng `yarn install --frozen-lockfile` như Docker OK.

**CHƯA kiểm — việc đầu tiên trên MacBook:**
1. **Review Opus cho F0 chưa chạy** (orchestrator chỉ tự soát nhanh do người dùng cần dừng). Chạy agent `review` cho commit `2387026`, đối chiếu `deploy/` với `mdt-re-construct/deploy/app-core` (nếu có repo đó trên máy). Có điểm chặn ⇒ sửa bằng commit `fix(platform): F0 — ...`.
2. Compose dev + `/health/ready` = 200 với DB thật (mục 3 dưới).
3. ~~Frontend chạy cùng backend thật: `http://localhost:3280` hiện 2 chip "đang chạy"; `/abc` ⇒ 404 có "Về trang chủ"; 375px có bottom nav; bật/tắt chế độ tối.~~ **ĐÃ KIỂM 17/09/2026 (MacBook, Chrome headless, đợt tích hợp F2)** — đạt cả 4 ý.
4. `deploy/VERIFY-DOCKER.md` mục 0, 1, 3 (build ảnh bằng Docker). Mục 2 HTTPS cần server thật có DNS Cloudflare trỏ về.

## 3. Dựng môi trường dev trên MacBook

Yêu cầu: .NET SDK 10, Node ≥ 22.12, Yarn 1.22, Docker.

```bash
cp deploy/dev/.env.example deploy/dev/.env
docker compose -f deploy/dev/docker-compose.dev.yml --env-file deploy/dev/.env up -d
cp backend/services/identity-service/src/AntFarm.Identity.Api/appsettings.Development.json.example \
   backend/services/identity-service/src/AntFarm.Identity.Api/appsettings.Development.json
cp backend/services/chinese-backend/src/AntFarm.Chinese.Api/appsettings.Development.json.example \
   backend/services/chinese-backend/src/AntFarm.Chinese.Api/appsettings.Development.json
dotnet tool restore
dotnet run --project backend/services/identity-service/src/AntFarm.Identity.Api --launch-profile http   # 5281
dotnet run --project backend/services/chinese-backend/src/AntFarm.Chinese.Api --launch-profile http      # 5282
dotnet run --project backend/services/gateway --launch-profile http                                      # 5280
cd frontend && yarn install && yarn workspace @af/chinese dev                                            # 3280
```

Kiểm tra (đường dẫn kiểm tra sức khoẻ đi thẳng vào service, không qua gateway): `curl -s localhost:5281/health/ready` và `curl -s localhost:5282/health/ready` phải trả 200. Xem chi tiết ở `README.md`. Lưu ý: script `.sh` đã đặt quyền thực thi trong git; copy thủ công qua ổ đĩa thì có thể phải `chmod +x deploy/scripts/*.sh deploy/postgres/init/*.sh`.

## 4. Việc tiếp theo (theo hợp đồng §7–§8)

Thứ tự: F2 → F3 → F5 → F4 → F6 → F7 → F9 → F8 → F10 → F11 (F12 lên server, F13 portal để sau).

- **F2 — identity-service:** đăng ký/đăng nhập/refresh xoay vòng (cookie HttpOnly `af_rt`), JWT RS256 + JWKS, `AntFarm.Auth`, `@af/auth`, `@af/utils`, migration `F2_Accounts`. Chi tiết §5.1.1, §5.2.2, §5.3.1, §6.
- Lưu ý từ review F1 cho F2: interceptor refresh/401 phải đứng TRƯỚC bước `createApiClient` chuyển lỗi thành `ApiError` (còn cần `err.config` để gửi lại); chưa có route `/403`, sẽ thêm ở F3.
- Gợi ý review F1 chưa làm (không chặn): `ServiceStatusChip` `disabled` trong `Tooltip` gây cảnh báo console; `LangText`/`PageContainer` gộp `sx` bằng spread (hỏng khi `sx` là mảng/hàm); `buildTheme.ts:55` spread `extra.typography` thừa.

Quy trình mỗi feature: investigation → business-analysis bổ sung chi tiết nếu mục đang "mức thiết kế" → backend/database/content (Sonnet) ‖ frontend (**Fable**, luôn truyền `model: "fable"`) → review (Opus) → integration (Opus, commit riêng) → dừng báo người dùng (trừ khi được dặn làm một mạch).

## 5. Quyết định

**Đã chốt:** AntFarm/`@af`; mỗi ngôn ngữ một service + DB + app; identity-service + JWKS ở `id.antfarms.xyz`; `chinese.antfarms.xyz`; portal `antfarms.xyz` để sau (F13); mọi truy cập qua nginx biên → YARP; Docker bắt buộc (dev giai đoạn đầu chạy trực tiếp); HTTPS Let's Encrypt theo khuôn MedDental (SAN, HTTP-01, RSA); DNS ở Cloudflare (SSL mode Full (strict)); HSK 3.0 cấp 1; FSRS-6; frontend do agent Fable làm; D19 giữ tiền tố `/chinese/api` (mặc định BA, người dùng chưa phản đối).

**Còn mở (hợp đồng §10):** D3 dữ liệu nét chữ CDN/đóng gói (chặn F8) · D4 đăng ký mở/mời · D5 nguồn nghĩa tiếng Việt (chặn F6) · D6 chấp nhận CC BY-SA cho dữ liệu (chặn F6) · D7 luật hoàn thành bài · D8 định nghĩa "ngày có học" · D20 chứng chỉ wildcard qua DNS-01 Cloudflare (F12).
