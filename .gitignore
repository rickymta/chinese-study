# ── .NET ─────────────────────────────────────────────────
bin/
obj/
publish/
.vs/
*.user
*.suo
*.DotSettings.user
TestResults/
coverage/
*.nupkg

# ── Secrets & cấu hình nhạy cảm ─────────────────────────
**/appsettings.Development.json
**/appsettings.Local.json
**/appsettings.Production.json
**/secrets.json
.env
.env.local
.env.*.local
*.pfx
*.pem

# ── Node / frontend ─────────────────────────────────────
node_modules/
dist/
*.tsbuildinfo
.turbo/
.vite/

# ── IDE / OS ────────────────────────────────────────────
.idea/
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db

# ── Claude Code cục bộ ──────────────────────────────────
.claude/settings.local.json

# ── Khoá ký identity-service (dev) ──────────────────────
.secrets/

# ── Học liệu — dữ liệu thô chưa xử lý + tool riêng ───────
content/**/.raw/
content/node_modules/

# ── Triển khai — bí mật/chứng chỉ không commit ───────────
deploy/.env
deploy/dev/.env
deploy/certs/
deploy/conf/nginx.conf
deploy/conf/cloudflare-realip.conf
deploy/secrets/

